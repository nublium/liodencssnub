@keyframes nubdoll {
    0%, 100% { transform: translateY(0) rotate(-2deg); }
    50% { transform: translateY(-10px) rotate(2deg); }
}

body::after {
    content: "";
    position: fixed;
    right: 18px;
    bottom: 14px;
    width: 220px;
    height: 180px;
    background: url('https://nublium.github.io/liodencssnub/carahmirpagedoll.png') no-repeat bottom center;
    background-size: contain;
    transform-origin: bottom center;
    animation: nubdoll 6s ease-in-out infinite;
    filter: drop-shadow(0 5px 10px rgba(0, 0, 0, .45));
    pointer-events: none;
    z-index: 9998;
}

@media (max-width: 1100px) {
    body::after {
        width: 150px;
        height: 123px;
        right: 8px;
        bottom: 8px;
        opacity: .9;
    }
}

@media (max-width: 700px) {
    body::after { display: none; }
}

@media (prefers-reduced-motion: reduce) {
    body::after { animation: none; }
}
